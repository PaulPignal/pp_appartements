name: appartements
version: 1.0.0
config-version: 2

profile: appartements

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

vars:
  dvf_raw_glob: "data/dvf/raw/*.txt"

models:
  appartements:
    +materialized: view
    staging:
      +schema: staging
    marts:
      +schema: marts
